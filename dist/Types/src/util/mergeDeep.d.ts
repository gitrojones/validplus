/**
 * Deep merge two objects, left most takes priority
 *
 * @param target - The object to merge onto
 * @param sources - The object(s) to merge into the target
 */
export declare const mergeDeep: (target: any, ...sources: any[]) => object;
